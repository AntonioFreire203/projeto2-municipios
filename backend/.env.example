# Servidor
PORT=3001
CORS_ORIGIN=http://localhost:5173
LOG_LEVEL=info

# PostgreSQL (pool de conexoes)
DB_HOST=localhost
DB_PORT=5432
DB_USER=municipios
DB_PASSWORD=municipios
DB_NAME=municipios
DB_POOL_MAX=10

# Redis (cache + blacklist de tokens)
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET=troque-este-segredo-por-um-valor-aleatorio-e-longo
JWT_EXPIRES_IN=1h
